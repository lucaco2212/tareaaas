[project]
name = "reganometro"
version = "0.1.0"
description = "Un vigilante de inactividad que te regaña cuando dejas el computador abandonado."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Tareaaas" }]

[project.scripts]
reganometro = "reganometro.app:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
